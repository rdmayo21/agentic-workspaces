# Decisions

Settled choices, append-only — never edit or delete an entry; supersede with a
new one. Include the why — future sessions can't ask.

Use headed sections, not table rows: long rationales in table cells defeat grep
(one hit = one unreadable mega-line) and offset/limit reads.

### {{date}} — Created this workspace

**Decision:** Workspace bootstrapped from the base template.
**Rationale:** (initial setup)
**Outcome:** done.
