# Next actions

Three short lists. Done items leave this file (to DECISIONS.md if they
settled something, otherwise just deleted — git remembers). This is a
to-do list, not a history.

## Now (max 3)

The only items a session may work on without being asked.

1. Fill in the "What this is" section of `AGENTS.md`, then replace the
   placeholders in `STATUS.md`.
2. *(open slot)*
3. *(open slot)*

## Waiting on

- (dated triggers or pending replies — `YYYY-MM-DD — what we're waiting
  for, and what to do when it arrives`)

## Parked

- (ideas not being pursued right now, one line each)
