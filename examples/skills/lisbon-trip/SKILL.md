---
name: lisbon-trip
description: Lisbon trip workspace (May 8–15, 2027) — flights, apartment, day plans, budget. USE THIS SKILL for anything about the Lisbon trip, Sintra, the Porto side trip, the apartment booking, or when the user asks "where are we on the trip", "trip status", "what's next for the trip". Invoke with /lisbon-trip.
---

The workspace for this project is `~/ai-workspaces/lisbon-trip/`. Read its
`AGENTS.md`, then `STATUS.md` and `NEXT-ACTIONS.md`, before answering
anything. Follow the update discipline in `AGENTS.md` after meaningful work,
then run `python3 ~/ai-workspaces/skills/new-ai-workspace/scripts/sync.py now`
so the record is backed up. That is the whole skill.
